# BookmarkX 产品需求文档（PRD）

- **产品名称**：BookmarkX
- **平台**：macOS（原生应用）
- **版本**：v1.0（首个版本）
- **文档状态**：草案
- **最后更新**：2026-08-11

---

## 1. 产品概述

### 1.1 一句话定位

BookmarkX 是一款本地优先（Local-First）的 macOS 原生应用，帮助 X（Twitter）重度用户备份、整理、检索自己的书签，并借助 Grok 模型实现自动分类、内容摘要与智能打标，把零散的推文沉淀为个人知识库。

### 1.2 背景与问题

X 内置的书签功能非常薄弱：

- 只有一个扁平列表，无文件夹、无标签（免费用户）；
- 没有全文搜索，书签一多就找不到内容；
- 推文可能被删除、账号可能被封禁，收藏的内容随时会丢失；
- 无法导出，内容无法进入个人的笔记 / 知识管理工作流。

市面上的解决方案（如 Twillot、Dewey）以浏览器插件 + 云服务为主，存在浏览器绑定、数据上云的隐私顾虑。BookmarkX 的差异化是：**macOS 原生体验 + 数据完全存储在本地 SQLite + 用 Grok 做智能整理**。

### 1.3 目标用户

- AI / 科技资讯的重度收藏者，希望把 X 上的知识沉淀下来；
- 内容创作者、记者、写作者：收集素材与灵感；
- 研究人员 / 分析师：跟踪话题、导出数据集;
- 投资者 / Web3 用户：汇总项目信息与 KOL 观点。

### 1.4 v1.0 范围（对标 Twillot 核心能力）

| 能力 | v1.0 | 说明 |
| --- | --- | --- |
| X 账号登录 | ✅ | 与 Grok（xAI）集成的登录流程 |
| 书签全量同步到本地 | ✅ | 增量更新，SQLite 存储 |
| AI 自动分类 | ✅ | Grok 模型按主题归类 |
| AI 内容摘要 | ✅ | 每条书签生成摘要 |
| AI 自动打标签 | ✅ | 可编辑、可增删 |
| 文件夹 / 分组管理 | ✅ | 无限层级，拖拽移动 |
| 全文搜索 | ✅ | 本地索引，毫秒级，多语言 |
| 编辑与整理 | ✅ | 编辑标签 / 摘要 / 备注，移动分组 |
| 多语言界面 | ✅ | 简体中文、英文起步 |
| 导出（MD / CSV / JSON） | ✅ | 对标 Twillot 导出能力 |
| 媒体中心（图片/视频墙） | 🔜 v1.1 | 瀑布流浏览与批量下载 |
| 点赞 / 推文历史备份 | 🔜 v1.1 | 先聚焦书签 |
| 匿名浏览器 | ❌ 不做 | 与本地书签管理定位不符 |
| 云同步 | ❌ 不做 | 坚持本地优先 |

---

## 2. 核心功能需求

### 2.1 登录与授权

**目标**：用户用自己的 X 账号完成授权，同时配置 Grok（xAI）的模型访问，实现「Grok 与 X 的集成登录」。

- **F-2.1.1** 首次启动引导用户完成两步连接：
  1. **连接 X 账号**：获取用户书签数据的访问凭证；
  2. **连接 Grok**：配置 xAI API Key（xAI 账号支持用 X 账号直接登录，引导中说明）。
- **F-2.1.2** 凭证安全存储于 macOS Keychain，不落盘明文。
- **F-2.1.3** 登录状态过期时给出明确提示并引导重新授权。
- **F-2.1.4** 支持登出并清除本地凭证（可选择是否保留已同步数据）。

### 2.2 书签同步

**目标**：把用户 X 账号下的全部书签完整同步到本地 SQLite。

- **F-2.2.1** 首次同步：拉取全部书签（含推文正文、作者信息、发布时间、媒体 URL、指标数据、外链）。
- **F-2.2.2** 增量同步：手动触发 + 定时自动同步，按游标 / 去重合并。
- **F-2.2.3** 同步进度可视化：已同步数量、当前状态、失败重试。
- **F-2.2.4** 媒体文件（图片、视频封面）缓存到本地，推文被删后内容仍可查看。
- **F-2.2.5** 对 API 限流有退避重试策略，同步可中断续传。

### 2.3 Grok 智能整理

**目标**：同步完成后，使用 Grok 模型自动整理全部书签。

- **F-2.3.1 自动分类**：Grok 按主题将书签归入分类（如 AI、编程、投资、设计……），分类体系支持用户自定义与合并。
- **F-2.3.2 自动摘要**：为每条书签（含长推 / 线程）生成简洁摘要，摘要语言跟随界面语言设置。
- **F-2.3.3 自动标签**：为每条书签生成 1–5 个标签；标签全局去重、可重命名、可合并。
- **F-2.3.4** 批量处理：新同步的书签自动进入 AI 处理队列，展示处理进度；失败可重试。
- **F-2.3.5** 成本控制：批量请求合并、结果缓存，同一条推文不重复处理；可配置使用的 Grok 模型（如 grok-4 / grok-4-fast）。
- **F-2.3.6** AI 结果均可人工修改，人工修改的内容在重新处理时不被覆盖。

### 2.4 组织与编辑

- **F-2.4.1** 文件夹：无限层级，支持创建 / 重命名 / 删除 / 拖拽排序。
- **F-2.4.2** 移动：单条或批量把书签移动到文件夹 / 分类。
- **F-2.4.3** 标签编辑：增删改标签，支持颜色标记。
- **F-2.4.4** 备注：每条书签可添加个人备注。
- **F-2.4.5** 批量操作：多选后批量打标 / 移动 / 删除。

### 2.5 搜索与浏览

- **F-2.5.1** 全文搜索：基于 SQLite FTS5，覆盖推文正文、作者、摘要、标签、备注；支持中文 / 英文 / 日文等多语言分词；关键词高亮。
- **F-2.5.2** 筛选：按作者、日期区间、媒体类型、分类、标签、文件夹组合筛选。
- **F-2.5.3** 视图：列表视图 + 表格视图；按时间 / 作者 / 互动数排序。
- **F-2.5.4** 详情页：查看推文全文、线程上下文、媒体、原文链接跳转。

### 2.6 导出

- **F-2.6.1** 支持导出为 Markdown、CSV、JSON。
- **F-2.6.2** 支持按筛选结果 / 文件夹 / 多选范围导出。
- **F-2.6.3** Markdown 导出兼容 Obsidian（含 frontmatter 元数据）。

### 2.7 多语言

- **F-2.7.1** 界面语言：v1.0 提供简体中文、English，跟随系统或手动切换。
- **F-2.7.2** AI 输出语言（摘要 / 分类名 / 标签）可独立设置。
- **F-2.7.3** 架构上使用标准 localization 方案，便于后续扩展语种。

---

## 3. 技术方案

### 3.1 技术栈

| 层 | 选型 | 说明 |
| --- | --- | --- |
| 应用框架 | Swift + SwiftUI | macOS 原生，目标 macOS 14+ |
| 数据库 | SQLite（GRDB.swift） | 本地存储 + FTS5 全文索引 |
| AI | xAI Grok API | OpenAI 兼容接口，structured output |
| X 数据 | X API v2 bookmarks 端点（OAuth 2.0 PKCE） | 详见 3.3 风险 |
| 凭证存储 | macOS Keychain | Token / API Key |
| 本地化 | String Catalogs (.xcstrings) | zh-Hans / en |

### 3.2 数据模型（核心表）

- `tweets`：推文主体（id、正文、作者、时间、指标、原始 JSON）
- `authors`：作者（id、handle、昵称、头像）
- `media`：媒体（tweet_id、类型、远程 URL、本地缓存路径）
- `bookmarks`：书签关系（tweet_id、收藏时间、folder_id、备注）
- `folders`：文件夹（父子层级、排序、颜色）
- `tags` / `bookmark_tags`：标签与多对多关系（含 AI 生成标记位）
- `ai_results`：AI 处理结果（摘要、分类、模型版本、是否人工修改）
- `tweets_fts`：FTS5 虚拟表（正文、摘要、标签、备注）

### 3.3 关键风险与对策

| 风险 | 说明 | 对策 |
| --- | --- | --- |
| X API 书签端点的配额与费用 | 免费档配额极低，Basic 档收费 | v1.0 用户自备 X API 凭证；评估浏览器会话导入作为备选方案 |
| Grok API 成本 | 大量书签批量处理消耗 token | 批量合并请求、结果缓存、可选轻量模型 |
| 推文内容版权与合规 | 本地备份仅限个人使用 | 明确仅存储用户自己收藏的内容，不提供公开分享 |
| AI 分类质量 | 主题漂移、标签过散 | 受控标签词表 + 用户反馈修正 |

---

## 4. 非功能需求

- **性能**：1 万条书签规模下，搜索响应 < 100ms，列表滚动流畅（60fps）。
- **隐私**：所有书签数据仅存储在本地；网络请求仅发生在 X 同步与 Grok API 调用；不接入任何统计 / 追踪 SDK。
- **可靠性**：同步与 AI 处理均可中断恢复；数据库自动备份（本地快照）。
- **可用性**：首次上手 3 步内完成（登录 X → 配置 Grok → 开始同步）。

---

## 5. 里程碑

| 阶段 | 内容 | 
| --- | --- |
| M1 | 项目骨架、数据库 Schema、X 登录与书签同步 |
| M2 | 书签列表 / 详情 / 搜索 / 文件夹与标签管理 |
| M3 | Grok 集成：自动分类、摘要、打标 + 处理队列 |
| M4 | 导出、多语言、设置页、打磨与发布（v1.0） |

---

## 6. 未来版本方向（Backlog）

- 点赞、推文历史的备份与整理（v1.1）
- 媒体中心：瀑布流浏览、批量下载（v1.1）
- 活动热力图（类 GitHub contribution 图）
- PDF 导出、Google Drive / Obsidian 同步
- 语义搜索（本地向量索引）
- 屏蔽列表管理
